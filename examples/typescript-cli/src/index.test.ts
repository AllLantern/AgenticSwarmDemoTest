import { describe, it, expect } from 'vitest'

/**
 * Test suite for Demo CLI
 *
 * TDD Guide will add tests here BEFORE implementation!
 */

describe('Demo CLI', () => {
  it('should have tests written first', () => {
    // This is where TDD-GUIDE will add failing tests
    // Then we implement to make them pass!
    expect(true).toBe(true)
  })

  // Example of what tests might look like:
  //
  // describe('when organizing files', () => {
  //   it('should group files by extension', () => {
  //     const files = ['a.txt', 'b.pdf', 'c.txt']
  //     const result = organizeByExtension(files)
  //     expect(result).toEqual({
  //       txt: ['a.txt', 'c.txt'],
  //       pdf: ['b.pdf']
  //     })
  //   })
  //
  //   it('should handle files without extension', () => {
  //     const files = ['README', 'Makefile']
  //     const result = organizeByExtension(files)
  //     expect(result).toEqual({
  //       'no-extension': ['README', 'Makefile']
  //     })
  //   })
  // })
})
